const AuthHeader = ({ title, subtitle }) => (
  <>
    <h2 className="text-2xl font-bold text-text text-center">{title}</h2>
    <p className="text-sm text-muted text-center mt-2">{subtitle}</p>
  </>
);

export default AuthHeader